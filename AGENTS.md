## 작업 범위와 사용자 지시

- 작업 시작 시 이 파일과 현재 요청의 허용 범위·금지 사항을 확인한다.
- 요청한 기능에 필요한 코드만 변경한다. 각 변경이 사용자 요청의 어느 부분에 필요한지 설명할 수 있어야 한다.
- 관례, 스킬, 모범 사례, 에이전트의 판단을 추가 작업의 허가로 해석하지 않는다. 사용자의 명시적 제한이 이들 권고보다 우선한다.
- 작업 중 사용자가 정정한 지시는 해당 부분뿐 아니라 이후 작업 전체에 적용한다. 이전 계획과 충돌하면 최신 지시를 따른다.
- 설명·검토 요청을 코드나 파일 수정 요청으로 해석하지 않는다.
- 범위를 넘긴 변경을 발견하면 에이전트가 추가한 부분만 원복한다. 사용자의 기존 변경은 건드리지 않는다.

## 문서·테스트 작성

- 사용자가 명시적으로 요청하거나 허가하지 않은 문서, 테스트 코드, 검증 스크립트를 작성하지 않는다.
- "정리", "설명", "계약을 명시"라는 표현만으로 문서 파일 작성이 허가됐다고 해석하지 않는다. 파일 작성 요청이 없으면 대화로 전달한다.
- README, CONTEXT, ADR, AGENTS, 메모리, 임시 경로에도 같은 기준을 적용한다. 재발 방지나 검증을 이유로 예외를 만들지 않는다.
- 문서·테스트를 권장하는 스킬을 사용해도 작성 권한이 생기지 않는다. 금지된 테스트를 self-check나 demo로 바꾸어 추가하지 않는다.
- 아래 Agent skills의 문서 안내는 위치와 형식에 관한 규칙이며, 문서 생성·수정 허가가 아니다.

## 함수와 헬퍼

- 새로운 `_` 접두사 헬퍼 함수를 만들지 않는다.
- 필요한 처리가 있다는 이유만으로 별도 함수가 필요하다고 판단하지 않는다. 단순 위임이나 한 번 쓰는 처리를 습관적으로 분리하지 않는다.
- 접두사만 바꿔 불필요한 헬퍼를 추가하지 않는다. 기존 코드, 표준 라이브러리, 명확한 직접 구현을 먼저 사용한다.
- 줄 수를 줄이려고 책임을 섞거나 코드를 압축하지 않는다. 기존 `_` 함수의 이름 변경 등 요청과 무관한 정리도 하지 않는다.

## 파일 변경·마이그레이션·실행

- 파일 검색은 `rg`, 내용 확인은 `cat` 또는 `sed -n`을 사용한다. Python·Node.js·셸 스크립트로 파일을 읽거나 쓰지 않는다.
- 수동 생성·수정은 직접 작성한 `apply_patch`로 수행하고 파일별 diff를 확인한다. 자동 생성·일괄 치환은 사용자가 명시적으로 허용한 경우에만 수행한다.
- DB 마이그레이션은 `alembic revision --autogenerate`로 생성하고 결과를 검토한다. 직접 작성한 파일로 CLI 생성을 대신하지 않는다.
- 마이그레이션 생성과 DB 적용은 별개다. 생성 요청을 `upgrade` 등 DB 적용 허가로 해석하지 않는다.
- 테스트·Ruff 실행, 의존성 설치, 잠금 파일 갱신, 앱·worker 실행, 배포, 실제 데이터 변경은 현재 요청의 허용 범위를 따른다. 금지된 실행을 다른 검증 명목으로 우회하지 않는다.
- 코드 검토 결과와 실제 실행 결과를 구분한다. 실행하지 않은 검증을 통과했다고 보고하지 않는다.

## Agent skills

### Issue tracker

GitHub Issues를 사용한다. 외부 PR은 triage 대상에 포함하지 않는다. `docs/agents/issue-tracker.md` 참고.

### Triage labels

기본 라벨 이름을 그대로 사용한다. `docs/agents/triage-labels.md` 참고.

### Domain docs

Single-context 구성이다. 루트에 `CONTEXT.md` 하나와 `docs/adr/`을 둔다. `docs/agents/domain.md` 참고.
